An insecure application.

Don't host this publicly unless you want to provide a remote shell to hackers.

## Development

Work from the src directory,

### Setup

    apt-get install python3-dev python3-virtualenv
    virtualenv --python `which python3` env
    source env/bin/activate
    pip install -r requirements.txt

### General dev.

    DATABASE_URL=fixme APP_SETTINGS=config.DevelopmentConfig python run.py

#### Tests

    DATABASE_URL=fixme APP_SETTINGS=config.DevelopmentConfig python -m unittest

### DB Management

#### DB setup

To create or update the database to the latest version run the upgrade command.

    DATABASE_URL=fixme APP_SETTINGS=config.DevelopmentConfig python manage.py db upgrade

#### DB changes

To create a migration file for database changes run the migrate command.

    DATABASE_URL=fixme APP_SETTINGS=config.DevelopmentConfig python manage.py db migrate
